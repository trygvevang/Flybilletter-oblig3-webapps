<div class="list-group">
        <div class="list-group-item" *ngFor="let category of allCategories">
            <div role="heading">
                <h4 class="panel-title">
                    <a data-toggle="collapse" href="#{{category.Type + '_ul'}}">{{category.Type}}</a>
                </h4>
            </div>

            <div class="collapse" id="{{category.Type + '_ul'}}">
                <h4 *ngIf="!category.Questions" class="panel-title">Det er ingen spørsmål i kategorien enda.</h4>
                <div *ngIf="category.Questions">
                    <div *ngFor="let question of category.Questions">
                        <a data-toggle="collapse" href="#{{category.Type + '_' + question.ID + '_ul'}}">{{question.Quest}}</a>
                        <div class="panel-collapse collapse" id="{{category.Type + '_' + question.ID + '_ul'}}">
                            <div *ngIf="question.Person">Stilt av: {{question.Person}}</div>
                            <div *ngIf="question.Answer">Svar: {{question.Answer}}</div>
                            <div *ngIf="!question.Answer">
                                <button *ngIf="!isAnsweringQuestion" class="btn btn-danger btn-xs" (click)="showAnswerQForm()">Besvar spørsmål</button>
                                <form *ngIf="isAnsweringQuestion" [formGroup]="answerForm" (ngSubmit)="answerQuestion(question)">
                                    <input type="text" formControlName="Answer" />
                                    <button type="submit" class="btn btn-success btn-xs">Besvar</button> <!--[disabled]="!form.valid"-->
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</div>